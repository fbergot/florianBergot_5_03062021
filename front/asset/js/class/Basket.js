import Utils from "./Utils.js";
import LocalStorage from "./LocalStorage.js";
/**
 * all for the basket
 * @export
 * @class Basket
 */
export default class Basket {
    /**
     * add product in basket
     * @static
     * @param {Object} product
     * @memberof Basket
     */
    static addInBasket(product) {
        console.log(product);
    }
}